import { createGlobalStyle } from "styled-components";

export default createGlobalStyle`
  :root {
    --primary-color: #9774CC;
    --secondary-color: #FFABEA;
    --white: #FFFFFF;
  }

  body {
    font-family: 'Lato', sans-serif;
  }

`;
